/ <% provide(:title, 'All users') %>
/ <h1>All users</h1>

/ <%= will_paginate %>

/ <ul class="users">
/   <%= render @users %>
/ </ul>

/ <%= will_paginate %>

- provide(:title, 'All users')
h1 All users
= will_paginate
ul.users
  = render @users
= will_paginate
